<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="font-serif bg-gray-100">
    <header class="bg-gray-800 text-white p-4 flex justify-between items-center">
      <div class="text-lg font-semibold">Learning Management System</div>
      <a href="/" class="bg-red-500 hover:bg-red-700 text-white py-2 px-4 rounded">Home</a>
    </header>

    <div class="container mx-auto p-6">
      <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2 bg-white border border-gray-200 rounded-lg shadow-md p-6">
          <h1 class="text-2xl font-bold text-gray-800 mb-4">Login to Your Account</h1>
          <% if (message && message.error && message.error.length > 0) { %>
            <% for (var i = 0; i < message.error.length; i++) { %>
              <li class="pt-2 text-red-600" type="none"><%= message.error[i] %></li>
            <% } %>
          <% } %>
          <form action="/session" method="post">
            <div class="mb-4">
              <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
              <input
                type="email"
                name="email"
                id="email"
                placeholder="Email"
                class="border border-gray-300 text-sm rounded p-2 text-gray-900 w-full"
                required
              />
            </div>
            <div class="mb-4">
              <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
              <input
                type="password"
                name="password"
                id="password"
                autocomplete="off"
                placeholder="Password"
                class="border border-gray-300 text-sm rounded p-2 text-gray-900 w-full"
                required
              />
            </div>
            <div class="flex justify-end">
              <button
                type="submit"
                class="bg-gray-800 hover:bg-gray-600 text-white px-5 py-2 rounded font-medium"
              >
                Sign In
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
