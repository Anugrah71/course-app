<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %> - <%= page.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-serif bg-gray-100 font-sans">
    <header class="bg-gray-800 text-white p-4 flex justify-between items-center">
        <div class="text-lg font-semibold">Learning Management System</div>
        <a href="/signout" class="bg-red-500 hover:bg-red-700 text-white py-2 px-4 rounded">Signout</a>
    </header>

    <div class="container mx-auto p-6">
        <h1 class="text-2xl font-bold text-gray-800 mb-6"><%= course.name %> - <%= page.title %></h1>
        <p class="text-sm text-gray-600 mb-4">Educator: <%= course.educator.name %></p>
        <p class="text-sm text-gray-600 mb-4">Description: <%= course.description %></p>
        <p class="text-sm text-gray-600 mb-4">Enrollments: <%= course.enrollments.length %></p>
        <h2 class="text-xl font-semibold text-gray-700 mb-4">Page Content</h2>
        <div class="prose">
            <%= page.content %>
        </div>
        
        <div class="flex justify-between mt-6">
            <button class="bg-green-600 hover:bg-green-800 text-white py-2 px-4 rounded">Completed</button>
            <button class="bg-green-600 hover:bg-green-800 text-white py-2 px-4 rounded">Next &#8594;</button>
        </div>
    </div>
</body>
</html>