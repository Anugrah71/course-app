<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-serif bg-gray-100 font-sans">
    <header class="bg-gray-800 text-white p-4 flex justify-between items-center">
        <div class="text-lg font-semibold">Learning Management System</div>
        <a href="/signout" class="bg-red-500 hover:bg-red-700 text-white py-2 px-4 rounded">Signout</a>
    </header>

    <div class="container mx-auto p-6">
        <h2 class="text-2xl font-bold mb-4"><%= course.name %></h2>
        
        <% course.chapters.forEach(chapter => { %>
            <h3 class="text-lg font-semibold border-b border-gray-300 pb-2 mb-3"><%= chapter.title %></h3>
            <ul>
                <% chapter.pages.forEach(page => { %>
                    <li class="flex justify-between text-gray-700">
                        <a href="/page/<%= page.id %>" class="text-blue-600 hover:underline"><%= page.title %></a>
                        <span class="text-green-600">Completed</span>
                    </li>
                <% }) %>
            </ul>
        <% }) %>
    </div>
</body>
</html>